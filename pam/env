# environment variables constant for the duration of the login session

# XDG config home
XDG_CONFIG_HOME DEFAULT=@{HOME}/.config

# sudo editor
SUDO_EDITOR DEFAULT=vim

# path
#PATH DEFAULT="@{HOME}/bin:${PATH}"

# zsh directory
ZDOTDIR DEFAULT=${XDG_CONFIG_HOME}/zsh

# bash prompt
GIT_PS1_SHOWDIRTYSTATE DEFAULT=true
GIT_PS1_SHOWSTASHSTATE DEFAULT=true
GIT_PS1_SHOWUNTRACKEDFILES DEFAULT=true
GIT_PS1_SHOWUPSTREAM DEFAULT=auto

# environment variables for sway
XKB_DEFAULT_MODEL DEFAULT=inspiron
XKB_DEFAULT_LAYOUT DEFAULT=us,us
XKB_DEFAULT_VARIANT DEFAULT=dvp,
XKB_DEFAULT_OPTIONS DEFAULT=ctrl:nocaps,grp:alt_shift_toggle

# SSH agent
SSH_AUTH_SOCK DEFAULT="${XDG_RUNTIME_DIR}/ssh-agent.socket"

# SSH agent emulation with GPG agent
#SSH_AGENT_PID DEFAULT=
#SSH_AUTH_SOCK DEFAULT=${XDG_RUNTIME_DIR}/gnupg/S.gpg-agent.ssh

# Z3
#LD_LIBRARY_PATH DEFAULT=/usr/lib
#PYTHONPATH DEFAULT=/usr/lib/python3.6/site-packages
